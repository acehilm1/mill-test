# Backend secrets — never commit real values.
# Used by the /api/contact route. Leave blank in local dev to log instead of send.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO=hello@example.com